1.在内核中先创建出共享内存
通过shmget(key,size,shmlf)函数创建
 key 就是一个身份表示符，通过key就能找到一个共享的内存对象
 在内核中可以创建很多共享内存，使用不同的key进行区分，键值对思想
 key需要按照一定的规则生成调用ftok(oathname,proj_id)函数生成
2.多个程序附加到这个共享内存上
shmat-->attach   将进程附加到共享内存上去，
3.就可以直接读取这个共享内存
